pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                sh 'echo "Hello from MacBook!"'
                sh 'aws --version || echo "no aws cli installed"'
                sh 'askjhasdhjkasdh'
            }
        }
    }

}